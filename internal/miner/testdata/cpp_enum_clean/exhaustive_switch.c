/* Clean: switch covers all Status members — no lead expected */

void handle(Status s) {
    switch (s) {
        case STATUS_OK:
            break;
        case STATUS_ERROR:
            break;
        case STATUS_DONE:
            break;
    }
}
